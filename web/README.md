Next.js frontend — not scaffolded yet. Per the build plan's lean-MVP scope,
this pass focused on the API (schema, matching, fare split, and the
concurrency-critical seat-claim endpoint). See the root README for what's
implemented and verified so far.
